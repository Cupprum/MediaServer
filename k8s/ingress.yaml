apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: media-server-ingress
spec:
  rules:
  - http:
      paths:
      - path: /heimdall
        pathType: Prefix
        backend:
          service:
            name: heimdall
            port:
              number: 8088
