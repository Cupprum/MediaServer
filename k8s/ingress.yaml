apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: media-server-ingress
spec:
  rules:
  - http:
      paths:
      - path: /qbittorrent
        pathType: Prefix
        backend:
          service:
            name: qbittorrent
            port:
              number: 8080
