# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: media-server-ingress
# spec:
#   rules:
#   - http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: heimdall
#             port:
#               number: 80
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /heimdall
      - /qbittorrent
      - /prowlarr
      - /jellyfin
