apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: media-server-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: monitoring-app-grafana
            port:
              number: 80